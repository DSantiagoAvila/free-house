import { Module } from '@nestjs/common';

/**
 * PaymentsModule — Future: payment gateway integration (Stripe, MercadoPago).
 * Depends on: OrdersModule, external payment provider SDK.
 */
@Module({})
export class PaymentsModule {}
