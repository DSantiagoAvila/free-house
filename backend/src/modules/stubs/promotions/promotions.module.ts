import { Module } from '@nestjs/common';

/**
 * PromotionsModule — Future: discount codes, seasonal promotions, price rules.
 * Depends on: ProductsModule (price calculation), OrdersModule (coupon application).
 */
@Module({})
export class PromotionsModule {}
