import { Module } from '@nestjs/common';

/**
 * AuthModule — Future: JWT authentication, login, register, refresh tokens.
 * Depends on: UsersModule, JWT strategy, bcrypt password hashing.
 */
@Module({})
export class AuthModule {}
