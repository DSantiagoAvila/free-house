import { Module } from '@nestjs/common';

/**
 * UsersModule — Future: customer profiles, addresses, order history.
 * Depends on: AuthModule (for guards), OrdersModule (for history).
 */
@Module({})
export class UsersModule {}
